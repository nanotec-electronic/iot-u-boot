# Findings — iot-u-boot

Review observations (bugs, drift, cleanup) awaiting a decision. Nanotec patch set only, not
upstream U-Boot. Add via the `add-task` skill (`add-task iot-u-boot finding "<text>"`).
Net-additions are gate-approved by the user.

## Open

## Planned

## Closed
